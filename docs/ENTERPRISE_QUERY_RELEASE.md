# 企业 SQL 查询发布门禁

`POST /api/enterprise/sqlagent/query-release` 用于把通过只读 SQL 策略的候选查询转为可审计的生产发布决策。

门禁覆盖上游策略结论、验证样例、字段血缘、参数绑定、敏感结果脱敏、超时、结果行数、扫描规模、定时任务审批和申请审批职责分离，返回 `APPROVE`、`REVIEW` 或 `REJECT`。每次评估生成稳定 SHA-256 发布指纹，可与审批单、执行日志和数据血缘关联。

本地实现不会连接真实数据库，也不调用外部模型；生产环境可在 `APPROVE` 后将运行时控制交给只读数据源代理执行。
